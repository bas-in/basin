# Basin — benchmark results

_Auto-generated by `benchmark/bundle.py`. Do not edit by hand._

- **Latest run:** 2026-05-23 23:08:28 UTC
- **Viability:** 39 / 40 passing
- **Scaling:** 7 / 9 passing
- **Postgres head-to-head:** 10 reports

For the live dashboard, open `benchmark/index_localfs.html` directly (no server needed). To regenerate after running tests:

```sh
cargo test -p basin-integration-tests --tests -- --nocapture
python3 benchmark/bundle.py
```

## Viability

| Test | Status | Measured | Bar |
|---|---|---|---|
| **ALTER TABLE ... ADD COLUMN end-to-end** | **PASS** | `10 rows` | `= 10` |
| **ALTER TABLE SQL surface** | **PASS** | `8 variants` | `= 8` |
| **Analytical path speedup vs OLTP engine** | **PASS** | `7.13×` | `≥ 1.5` |
| **Analytical routing speedup through basin-engine** | **PASS** | `6.48×` | `≥ 1.5` |
| **Cron-style scheduler runs jobs at minute boundaries** | **PASS** | `3 runs` | `= 3` |
| **Outbound HTTP surface (Postgres `http` + Supabase `pg_net`)** | **PASS** | `3 checks` | `= 3` |
| **Bloom filter pruning for absent point queries** | **PASS** | `0.9 fraction` | `≥ 0.8` |
| **Phase 5.7 A4: catalog-stored file-level stats prune fully-out-of-range queries with zero object-store IO** | **PASS** | `1 fraction` | `≥ 1` |
| **Cluster-by physical sort overhead on insert** | **PASS** | `0.97×` | `< 2` |
| **CLUSTER BY SQL surface (Phase 5.7 B2)** | **PASS** | `5 variants` | `= 5` |
| **Cold-start latency (LocalFS)** | **FAIL** | `3411.59 ms` | `< 2000` |
| **Audit-log Parquet vs CSV** | **PASS** | `77.69×` | `≥ 10` |
| **TimescaleDB-style continuous aggregates** | **PASS** | `5 buckets` | `= 5` |
| **NVMe disk cache (Phase 5.7 A1)** | **PASS** | `4.94 ms` | `< 250` |
| **Durable catalog: rows survive restart** | **PASS** | `1 bool` | `= 1` |
| **Postgres extended-query protocol works end-to-end** | **PASS** | `1 fraction` | `≥ 1` |
| **Idle-project RAM cost** | **PASS** | `2.06 KiB` | `< 500` |
| **Project isolation under concurrent load** | **PASS** | `0 leaks` | `= 0` |
| **JSONB column type** | **PASS** | `1 boolean` | `= 1` |
| **BYO-key envelope encryption** | **PASS** | `100 rows` | `= 100` |
| **Large-dataset point query** | **PASS** | `5.67 ms` | `< 100` |
| **Migration Manager v0.2 catalog ops end-to-end** | **PASS** | `5 tables` | `= 5` |
| **ORM-style query patterns work end-to-end** | **PASS** | `1 fraction` | `≥ 0.85` |
| **Parquet page cache (Phase 5.7 A2)** | **PASS** | `7.85 ms` | `< 75` |
| **Within-project time-based partition pruning** | **PASS** | `0.166568 ratio` | `< 0.2` |
| **bcrypt validation cost on pgwire connect** | **PASS** | `3.80 ms` | `< 300` |
| **Per-tenant connection-URL provision latency** | **PASS** | `4.64 ms` | `< 50` |
| **pg_trgm-compatible fuzzy text matching (similarity, word_similarity)** | **PASS** | `3 checks` | `= 3` |
| **PostGIS-compatible spatial subset (POINT, BOX2D, ST_Distance, ST_DWithin)** | **PASS** | `3 checks` | `= 3` |
| **Predicate pushdown vs full scan** | **PASS** | `1229888.00×` | `≥ 10` |
| **Project deletion latency** | **PASS** | `3.79 ms` | `< 3000` |
| **Row-level security: per-principal predicate injection** | **PASS** | `5 rows` | `= 5` |
| **RLS preserves project prefix isolation** | **PASS** | `0 rows` | `= 0` |
| **Per-table row-group sizing for point queries** | **PASS** | `0.0625 fraction` | `< 0.5` |
| **Shard-acked INSERT path** | **PASS** | `2971.67 inserts/sec` | `≥ 50` |
| **SQL compatibility (sqllogictest)** | **PASS** | `80 %` | `≥ 78` |
| **Per-table tiered storage (hot/cold)** | **PASS** | `1 files` | `= 1` |
| **UPDATE / DELETE via copy-on-write** | **PASS** | `17.08 ms` | `< 2000` |
| **UUID column type + uuid-ossp/pgcrypto built-in functions** | **PASS** | `1 boolean` | `= 1` |
| **Native vector search (HNSW + brute-force agree)** | **PASS** | `2.25×` | `≥ 1.3` |

- **ALTER TABLE ... ADD COLUMN end-to-end** — CREATE TABLE -> INSERT -> ALTER TABLE ADD COLUMN -> INSERT (wider) -> SELECT shows 10 rows: the 5 pre-ALTER rows project the new column to NULL (Parquet schema evolution), the 5 post-ALTER rows carry their inserted values. Measured `rows_visible_after_alter` = `10 rows`.
- **ALTER TABLE SQL surface** — Every ALTER form (ADD COLUMN, SET cold_after, SET cold_age_column, SET BLOOM FILTERS ON, ENABLE/DISABLE ROW LEVEL SECURITY, CREATE/DROP POLICY) parses, runs, and persists to the catalog. Measured `alter_variants_passed` = `8 variants`.
- **Analytical path speedup vs OLTP engine** — On a 1M-row aggregate (count + avg + sum-of-lengths) with GROUP BY across 10 Parquet files, the DuckDB-backed analytical engine beats the DataFusion-on-listing-table OLTP engine by at least 1.5x on the same dataset. Measured `analytical_speedup (engine_ms / analytical_ms)` = `7.13×`.
- **Analytical routing speedup through basin-engine** — Same SQL through the same engine entry point: with the analytical engine attached, GROUP BY queries route to DuckDB and finish at least 1.5x faster than the OLTP-only baseline on a 1M-row dataset spread across 10 Parquet files. Measured `analytical_routing_speedup (oltp_ms / routed_ms)` = `6.48×`.
- **Cron-style scheduler runs jobs at minute boundaries** — Three +60s ticks fire a `* * * * *` job exactly three times; markers and audit log match. Measured `runs_executed` = `3 runs`.
- **Outbound HTTP surface (Postgres `http` + Supabase `pg_net`)** — Sync GET round-trips, allowlist gate denies unknown hosts, async POST persists response row. Measured `checks_passed` = `3 checks`.
- **Bloom filter pruning for absent point queries** — On a multi-row-group Parquet file with `bloom_filter_columns = ["id"]`, an in-range point query for a non-existent id must prune at least 80% of row groups via the per-group bloom filter, even when min/max stats cannot. Measured `row_groups_pruned_by_bloom / row_groups_considered` = `0.9 fraction`.
- **Phase 5.7 A4: catalog-stored file-level stats prune fully-out-of-range queries with zero object-store IO** — Two parquet files with disjoint id ranges, predicate prunes both via catalog `column_stats`. `Storage::read_paths(empty)` issues zero GETs / HEADs / LISTs; the legacy `Storage::read` path through the same predicate observes non-zero IO (at minimum one footer fetch per file). Measured `fraction of legacy footer IO eliminated by A4 catalog-stats prune` = `1 fraction`.
- **Cluster-by physical sort overhead on insert** — Phase 5.7 B2 lexsorts every batch by the configured cluster columns before Parquet flush. The bar is < 2× slowdown vs an identical write with no cluster columns. Anything worse means the sort path got accidentally serialised or is paying per-row overhead instead of per-batch. Measured `cluster_mean / no_cluster_mean ratio` = `0.97×`.
- **CLUSTER BY SQL surface (Phase 5.7 B2)** — Every CLUSTER BY form (CREATE TABLE … CLUSTER BY, ALTER TABLE … CLUSTER BY, ALTER TABLE … RESET CLUSTER BY) parses, persists to the catalog, validates column membership, and the writer physically sorts flushed batches. Measured `cluster_by_variants_passed` = `5 variants`.
- **Cold-start latency (LocalFS)** — From process spawn to first `SELECT 1` over pgwire is < 2 s on LocalFS storage. Measured `cold_start_ms` = `3411.59 ms`.
- **Audit-log Parquet vs CSV** — Audit-log data is at least 10x smaller than CSV. Measured `ratio (csv / parquet)` = `77.69×`.
- **TimescaleDB-style continuous aggregates** — Auto-refreshed materialised view over a time-bucketed GROUP BY. The refresher rebuilds the materialisation on each interval tick; the CV appears as a regular table to readers. Measured `cv_buckets` = `5 buckets`.
- **NVMe disk cache (Phase 5.7 A1)** — Cold p99 of a 1000-iteration random-working-set point-query workload, behind a 50 ms-per-RPC latency injector standing in for cross-region S3, finishes under 250 ms. The 'cold' phase starts with both the disk cache and the in-process metadata cache empty (every working-set id is a miss); the 'warm' phase repeats the same workload (caches now populated). The bar is set against COLD p99 so an implementation can't pass by leaning on a pre-warmed cache. Measured `cold p99 ms` = `4.94 ms`.
- **Durable catalog: rows survive restart** — Catalog metadata persists across basin-server restarts. Measured `rows survived restart` = `1 bool`.
- **Postgres extended-query protocol works end-to-end** — tokio-postgres / asyncpg / JDBC default extended-query path runs CREATE / INSERT($1,$2) / prepared SELECT against Basin without falling back to simple_query. Measured `Extended-protocol queries that succeed` = `1 fraction`.
- **Idle-project RAM cost** — Basin holds many idle projects in one process for under 500 KiB each. Measured `per_project_kib` = `2.06 KiB`.
- **Project isolation under concurrent load** — Concurrent multi-project traffic produces zero cross-project row leakage. Measured `leaks` = `0 leaks`.
- **JSONB column type** — JSONB column accepts JSON-string literals on INSERT, stores them canonically (keys sorted, no whitespace) as LargeBinary, and returns them on SELECT as binary bytes that round-trip through serde_json. Different key orders on INSERT produce byte-identical stored payloads. Measured `jsonb_roundtrip_and_canonical` = `1 boolean`.
- **BYO-key envelope encryption** — MockProvider attached: bytes != plaintext, sidecar present, read returns input rows; back-compat with provider-less files preserved. Measured `rows round-tripped` = `100 rows`.
- **Large-dataset point query** — Cold p99 of a 1000-iteration random-working-set point-query workload on a 10 M-row, 10-file dataset finishes under 100 ms. Each query picks a different id from a fixed-seed pool of 1000 hot ids, so the cache is exercised at realistic granularity rather than re-asking the same row. The bar is on COLD p99 (page + disk cache empty at the start of the run); a 'warm' phase is reported alongside. Measured `cold p99 ms` = `5.67 ms`.
- **Migration Manager v0.2 catalog ops end-to-end** — Boot engine over InMemoryCatalog, CREATE 5 tables, INSERT first batch, capture wall time, INSERT second batch; list_snapshots_project_wide returns every table's snapshots in commit order; rollback_to_snapshot_project_wide(captured_time) rewinds every table to its first-batch state (count(*) = 3). Measured `tables_rolled_back` = `5 tables`.
- **ORM-style query patterns work end-to-end** — Seven representative query shapes that real ORMs (Diesel, SeaORM, Prisma, ActiveRecord, SQLAlchemy) emit through the Postgres extended-query protocol succeed against Basin. Failure mode is a clear protocol error, not a hang. Measured `Representative ORM patterns that succeed` = `1 fraction`.
- **Parquet page cache (Phase 5.7 A2)** — Cold p99 of a 1000-iteration random-working-set point-query workload finishes under 75 ms. The 'cold' phase starts with an empty page cache (every working-set id is a miss); the 'warm' phase repeats the same workload (caches now populated) and is reported alongside as corroboration. The bar is set against COLD p99 so no implementation can pass by leaning on a pre-warmed cache. Workload: 100k rows across 10 Parquet files, 1000 hot ids drawn at fixed PRNG seed. Measured `cold p99 ms` = `7.85 ms`.
- **Within-project time-based partition pruning** — A 1-month range query against 12 months of partitioned data reads at most one partition's worth of bytes (range/full bytes ratio < 0.2). Measured `partition_bytes_ratio (range / full)` = `0.166568 ratio`.
- **bcrypt validation cost on pgwire connect** — Every cold pgwire connection pays one bcrypt-verify on the way in. At test bcrypt-cost (4) p99 stays under 300 ms across 50 validations. Production cost-12 will run ~50× higher; the connection pool keeps this off the hot path for warm reuse. The number we publish here tracks regressions in the validation path itself, not in bcrypt's CPU envelope. Measured `p99 validate latency` = `3.80 ms`.
- **Per-tenant connection-URL provision latency** — Issuing a fresh pgwire (user, password) row + bcrypt hash + URL finishes within p99 < 50 ms at test bcrypt-cost (4). Production cost-12 is documented inline; this card tracks the test-cost number so a regression in the SQL / random-byte / URL-build path is caught. The bar is expressed against p99 across 100 provisions; mean / p50 are reported as distribution context. Measured `p99 provision latency` = `4.64 ms`.
- **pg_trgm-compatible fuzzy text matching (similarity, word_similarity)** — 100 typo pairs: min similarity >= 0.3 (PG default pg_trgm.similarity_threshold), max unrelated < 0.2, word_similarity('smyth', 'alyce smyth') >= 0.6. Measured `checks_passed` = `3 checks`.
- **PostGIS-compatible spatial subset (POINT, BOX2D, ST_Distance, ST_DWithin)** — 100 European points: Haversine within 1% of PostGIS ST_DistanceSphere; ST_DWithin matches truth set; WKB round-trip is byte-exact (21 B/point). Measured `checks_passed` = `3 checks`.
- **Predicate pushdown vs full scan** — A point query reads at least 10x less than a full scan would. Measured `reduction_x (full_scan_bytes / point_query_bytes)` = `1229888.00×`.
- **Project deletion latency** — Deleting a project of 100 small files via Storage::delete_project (catalog-first; LIST mop-up in parallel) completes in under 3 seconds (caches reset; cold path). Measured `deletion_ms` = `3.79 ms`.
- **Row-level security: per-principal predicate injection** — RLS isolates rows per `current_user` while preserving full visibility when disabled. Measured `alice_rows_visible` = `5 rows`.
- **RLS preserves project prefix isolation** — Cross-project row leakage is zero across every RLS configuration combination. Measured `cross_project_leak` = `0 rows`.
- **Per-table row-group sizing for point queries** — A table with `row_group_rows = 4096` scans strictly fewer than half the rows of an identical table at the default 65,536-row group size when answering a `WHERE id = X` point query. Measured `small_rg_scan_rows / default_rg_scan_rows` = `0.0625 fraction`.
- **Shard-acked INSERT path** — Single-row INSERTs route through the WAL + shard owner and acks fast enough that point INSERT throughput is no longer Parquet-bound. Measured `inserts_per_sec` = `2971.67 inserts/sec`.
- **SQL compatibility (sqllogictest)** — Curated PG-style .slt suite reaches >= 78% pass rate against basin-engine. Measured `pass_rate` = `80 %`.
- **Per-table tiered storage (hot/cold)** — Files older than the configured threshold migrate from the default 'hot' object-store prefix to a cheaper 'cold' prefix. Reads transparently follow the catalog regardless of tier. Measured `cold_files_after_sweep` = `1 files`.
- **UPDATE / DELETE via copy-on-write** — An UPDATE and a DELETE on a 10K-row table commit end-to-end (parser → executor → storage rewrite → catalog Replace snapshot) in under 2 seconds. Measured `UPDATE+DELETE elapsed (ms)` = `17.08 ms`.
- **UUID column type + uuid-ossp/pgcrypto built-in functions** — UUID column accepts canonical hyphenated literals and gen_random_uuid()/uuid_generate_v4() function calls on INSERT, stores 16 bytes RFC 4122, returns canonical hyphenated text on SELECT. digest()/encode()/decode() cover md5/sha1/sha224/sha256/sha384/sha512 + hex/base64/escape. crypt()/gen_salt('bf') produce bcrypt-shaped 60-char `$2b$NN$...` hashes that verify cleanly. Measured `uuid_full_surface` = `1 boolean`.
- **Native vector search (HNSW + brute-force agree)** — HNSW top-10 overlaps brute-force top-10 by >=7, HNSW serves the same query at <100ms. Measured `HNSW speed-up vs brute-force` = `2.25×`.

## Scaling

### Compute sharding (router -> shard owners) — **PASS**

_Hash project_id -> shard_id; pgwire connections route to the owning shard. Reconnects from the same project always land on the same shard, and load distributes evenly enough across shards._

**max shard load:** `31 %` (bar `< 50`)

| shard index | projects on shard (count) | share of projects (%) |
| --- | --- | --- |
| 0 | 19 | 19.00 |
| 1 | 31 | 31.00 |
| 2 | 21 | 21.00 |
| 3 | 29 | 29.00 |

### Concurrent reader fan-out — **PASS**

_Read throughput scales as more concurrent readers are added._

**peak speed-up vs C=1:** `8.87×` (bar `≥ 3.5`)

| concurrent readers | Total QPS (qps) | Per-task QPS (qps) | Median latency (us) |
| --- | --- | --- | --- |
| 1 | 34.67 | 34.67 | 27570.00 |
| 4 | 145.33 | 36.33 | 544.00 |
| 16 | 307.33 | 19.21 | 340.00 |
| 64 | 292.33 | 4.57 | 144281.00 |

### Single-project data size scale-up — **PASS**

_Storage is linear in row count; point-query latency stays bounded._

**point query p50 growth (10M / 100k):** `1.35×` (bar `< 5`)

| row count | Disk (MiB) | Bytes / row (B) | Point query p50 (ms) | Full scan p50 (ms) |
| --- | --- | --- | --- | --- |
| 100,000 | 0.78 | 8.21 | 2.61 | 5.20 |
| 1,000,000 | 7.82 | 8.20 | 2.51 | 50.79 |
| 10,000,000 | 78.23 | 8.20 | 3.52 | 536.31 |

### Idle-project cost curve — **PASS**

_RAM cost stays small per idle project as the project count grows._

**max per_project_kib across scales:** `3.68 KiB` (bar `< 5`)

| projects | Per-project RSS (KiB) | Provision time (ms) | RSS delta (KiB) |
| --- | --- | --- | --- |
| 100 | 3.68 | 0.19 | 368 |
| 1,000 | 2.02 | 1.54 | 2,016 |
| 5,000 | 1.73 | 7.66 | 8,672 |
| 10,000 | 2.14 | 17.81 | 21,408 |

### Noisy-neighbor degradation — **PASS**

_A heavy noisy project doesn't crater a quiet project's p99 latency._

**p99 ratio (under_load / baseline):** `0.43×` (bar `< 5`)

| scenario | p50 latency (ms) | p99 latency (ms) |
| --- | --- | --- |
| baseline | 0.35 | 0.74 |
| under_load | 0.12 | 0.32 |
| ratio | 0.33 | 0.43 |

### Full perf stack on a WAN-class point query — **FAIL**

_Same SELECT … WHERE id = X measured four ways under a 1000-iteration random-working-set point-query workload (1000 hot ids, fixed PRNG seed) with a 50 ms-per-RPC latency injector standing in for cross-region S3: (a) no cache, (b) +disk cache, (c) +page cache, (d) +bloom filter. Layer (a) shows the cold WAN cost (>200 ms p50), layer (d) lands cold p99 under 1500 ms (the tail is dominated by the fraction of iterations whose id wasn't yet cached and paid the full miss cost), and the headline claim is speedup ≥ 100× from (a)'s cold p50 to (d)'s cold p50._

**speedup p50 (a→d):** `1.53×` (bar `≥ 100`)

| Stack layer | p50 latency (ms) | p99 latency (ms) |
| --- | --- | --- |
| a_baseline | 224.87 | 284.95 |
| b_disk | 78.59 | 291.00 |
| c_disk_page | 145.38 | 653.04 |
| d_full | 146.55 | 278.15 |

### Per-project cost vs project count — **PASS**

_RAM and quiet point-query latency per project stay near-constant as project count grows._

**quiet_p50_at_max / quiet_p50_at_1:** `1.81×` (bar `< 5`)

| projects | Per-project RAM (KiB) | Quiet point query p50 (ms) |
| --- | --- | --- |
| 1 | 1088.00 | 0.21 |
| 10 | 120.89 | 0.21 |
| 100 | 42.49 | 0.20 |
| 1,000 | 22.49 | 0.38 |

### Project deletion at scale (Basin vs Postgres) — **PASS**

_Basin's project teardown is a bulk catalog DELETE plus a single drop_namespace; PG's DROP SCHEMA CASCADE walks every row and index. Basin's slope is structurally flatter, so it overtakes PG as the file count grows._

**crossover file count (Basin <= PG):** `100 files` (bar `< 5001`)

| files per project | Basin (ms) | Postgres (ms) |
| --- | --- | --- |
| 100 | 3.92 | 69.95 |
| 1,000 | 37.81 | 25.01 |
| 5,000 | 199.69 | 3.30 |

### Project deletion at scale, realistic SaaS schema (Basin vs Postgres) — **FAIL**

_Real production tables have 5-20 indexes and 1-3 FK constraints. PG's DROP SCHEMA CASCADE walks every row × every index + validates every FK on the cascade; Basin's project teardown stays O(file_count). The crossover from the simple card moves dramatically left under this schema profile._

**Basin/Postgres ratio at 5000 files (realistic schema):** `12.4536 ratio` (bar `< 1`)

| files per project | Basin (ms) | Postgres (ms) |
| --- | --- | --- |
| 100 | 4.34 | 5.99 |
| 1,000 | 85.79 | 22.44 |
| 5,000 | 198.03 | 15.90 |

## Postgres head-to-head

### Backup cost: Basin snapshot vs pg_dump (100k rows)

_Basin's Iceberg-style snapshot is an O(1) manifest copy while pg_dump is O(data)._

| Metric | Basin | Postgres | Winner | Ratio |
|---|---|---|---|---|
| Backup wall time | `0.00 s` | `0.38 s` | **Basin** | `pg / basin = 3128x` |
| Backup byte size | `0.00 MiB` | `6.08 MiB` | **Basin** | `pg / basin = 7751x` |

### Lifecycle ops: project deletion + ADD COLUMN

_Basin makes project teardown a catalog-first DELETE with a parallel orphan LIST and a single drop_namespace, and treats schema evolution as a catalog operation; PG must DROP SCHEMA CASCADE and (in the general case) rewrite the heap._

| Metric | Basin | Postgres | Winner | Ratio |
|---|---|---|---|---|
| Project deletion (1 project, 100K rows) | `6.10 ms` | `70.63 ms` | **Basin** | `pg / basin = 11.57x` |
| ADD COLUMN on 100K rows | `2.15 ms` | `1.97 ms` | **Postgres** | `pg / basin = 0.92x` |

### Basin vs Postgres 18 (1M-row SaaS+OLAP workload, no index)

_At 1M rows on the SaaS shape (users + events, joins, ILIKE, pagination, bulk DML, COUNT(*), DATE_TRUNC rollup, analytics JOIN), Basin's columnar substrate uses dramatically less disk than PG heap and wins the OLAP shapes while matching/beating PG on selective reads. No indexes either side — measures substrate, not btree machinery._

| Metric | Basin | Postgres | Winner | Ratio |
|---|---|---|---|---|
| On-disk bytes (users + events) | `2.62 MiB` | `76.73 MiB` | **Basin** | `pg / basin = 29.25x` |
| Point query p50 | `0.75 ms` | `12.21 ms` | **Basin** | `pg / basin = 16.31x` |
| Point query p99 | `51.79 ms` | `12.41 ms` | **Postgres** | `pg / basin = 0.24x` |
| Range scan p50 (~1k rows) | `0.74 ms` | `21.93 ms` | **Basin** | `pg / basin = 29.71x` |
| Range scan p99 | `15.18 ms` | `22.01 ms` | **Basin** | `pg / basin = 1.45x` |
| Aggregate GROUP BY user_id p50 | `500.99 ms` | `133.03 ms` | **Postgres** | `pg / basin = 0.27x` |
| 2-table JOIN GROUP BY p50 | `1187.21 ms` | `916.83 ms` | **Postgres** | `pg / basin = 0.77x` |
| ILIKE '%@gmail.com' p50 | `103.42 ms` | `24.83 ms` | **Postgres** | `pg / basin = 0.24x` |
| Pagination ORDER BY LIMIT/OFFSET p50 | `1015.54 ms` | `62.30 ms` | **Postgres** | `pg / basin = 0.06x` |
| Single-row UPDATE p50 | `4099.12 ms` | `2.64 ms` | **Postgres** | `pg / basin = 0.00x` |
| Bulk UPDATE (~333333 rows) | `714131.53 ms` | `1395.59 ms` | **Postgres** | `—` |
| DELETE WHERE id IN (10 rows) | `1040509.29 ms` | `56.67 ms` | **Postgres** | `—` |
| Bulk INSERT 1000000 rows | `146996.34 ms` | `4309.36 ms` | **Postgres** | `—` |
| Cold-start first query | `16.86 ms` | `19.43 ms` | **Basin** | `pg / basin = 1.15x` |
| COUNT(*) full table p50 | `0.23 ms` | `21.00 ms` | **Basin** | `pg / basin = 92.56x` |
| DATE_TRUNC day + SUM GROUP BY p50 | `440.50 ms` | `67.53 ms` | **Postgres** | `pg / basin = 0.15x` |
| Analytics JOIN+WHERE (last 333333s window) | `662.15 ms` | `670.37 ms` | **Basin** | `pg / basin = 1.01x` |
| COUNT(DISTINCT user_id) p50 | `381.29 ms` | `195.00 ms` | **Postgres** | `pg / basin = 0.51x` |
| LIKE 'pending%' prefix p50 | `0.66 ms` | `0.16 ms` | **Postgres** | `pg / basin = 0.25x` |
| Multi-col GROUP BY + HAVING p50 | `1272.74 ms` | `216.13 ms` | **Postgres** | `pg / basin = 0.17x` |
| Window LAG OVER PARTITION p50 | `643.79 ms` | `85.24 ms` | **Postgres** | `pg / basin = 0.13x` |
| Recursive CTE Fibonacci(30) p50 | `6.96 ms` | `0.09 ms` | **Postgres** | `pg / basin = 0.01x` |
| Correlated subquery in SELECT p50 | `418.94 ms` | `2729.71 ms` | **Basin** | `pg / basin = 6.52x` |
| EXISTS in WHERE p50 | `348.22 ms` | `139.68 ms` | **Postgres** | `pg / basin = 0.40x` |
| 3-table JOIN BETWEEN p50 | `1620.62 ms` | `263.70 ms` | **Postgres** | `pg / basin = 0.16x` |
| UNION ALL two scans p50 | `258.84 ms` | `64.89 ms` | **Postgres** | `pg / basin = 0.25x` |
| ORDER BY NULLS LAST + LIMIT p50 | `70.82 ms` | `18.86 ms` | **Postgres** | `pg / basin = 0.27x` |
| Top-N per group (MAX) p50 | `399.85 ms` | `131.34 ms` | **Postgres** | `pg / basin = 0.33x` |
| Numeric range BETWEEN p50 | `16.44 ms` | `19.62 ms` | **Basin** | `pg / basin = 1.19x` |

### Basin vs Postgres 18 (100k-row SaaS+OLAP workload, no index)

_At typical SaaS / OLTP scale (100k rows split across users + events), Basin's columnar substrate matches or beats Postgres heap on reads (point, range, aggregate, JOIN, ILIKE, pagination, COUNT(*), DATE_TRUNC, analytics JOIN) and pays a per-INSERT tax on bulk writes. No indexes on either side — measures substrate, not btree machinery._

| Metric | Basin | Postgres | Winner | Ratio |
|---|---|---|---|---|
| On-disk bytes (users + events) | `0.19 MiB` | `7.73 MiB` | **Basin** | `pg / basin = 40.77x` |
| Point query p50 | `0.92 ms` | `2.85 ms` | **Basin** | `pg / basin = 3.09x` |
| Point query p99 | `12.75 ms` | `2.86 ms` | **Postgres** | `pg / basin = 0.22x` |
| Range scan p50 (~1k rows) | `0.91 ms` | `5.51 ms` | **Basin** | `pg / basin = 6.08x` |
| Range scan p99 | `7.31 ms` | `5.60 ms` | **Postgres** | `pg / basin = 0.77x` |
| Aggregate GROUP BY user_id p50 | `36.98 ms` | `13.37 ms` | **Postgres** | `pg / basin = 0.36x` |
| 2-table JOIN GROUP BY p50 | `91.87 ms` | `45.53 ms` | **Postgres** | `pg / basin = 0.50x` |
| ILIKE '%@gmail.com' p50 | `18.02 ms` | `2.58 ms` | **Postgres** | `pg / basin = 0.14x` |
| Pagination ORDER BY LIMIT/OFFSET p50 | `90.48 ms` | `17.63 ms` | **Postgres** | `pg / basin = 0.19x` |
| Single-row UPDATE p50 | `158.28 ms` | `0.37 ms` | **Postgres** | `pg / basin = 0.00x` |
| Bulk UPDATE (~33333 rows) | `14602.58 ms` | `67.71 ms` | **Postgres** | `—` |
| DELETE WHERE id IN (10 rows) | `14519.75 ms` | `6.03 ms` | **Postgres** | `—` |
| Bulk INSERT 100000 rows | `9155.84 ms` | `377.96 ms` | **Postgres** | `—` |
| Cold-start first query | `14.64 ms` | `16.50 ms` | **Basin** | `pg / basin = 1.13x` |
| COUNT(*) full table p50 | `0.45 ms` | `5.08 ms` | **Basin** | `pg / basin = 11.37x` |
| DATE_TRUNC day + SUM GROUP BY p50 | `44.05 ms` | `19.02 ms` | **Postgres** | `pg / basin = 0.43x` |
| Analytics JOIN+WHERE (last 33333s window) | `70.81 ms` | `70.90 ms` | **Basin** | `pg / basin = 1.00x` |
| COUNT(DISTINCT user_id) p50 | `34.92 ms` | `37.73 ms` | **Basin** | `pg / basin = 1.08x` |
| LIKE 'pending%' prefix p50 | `52.15 ms` | `0.10 ms` | **Postgres** | `pg / basin = 0.00x` |
| Multi-col GROUP BY + HAVING p50 | `118.78 ms` | `40.84 ms` | **Postgres** | `pg / basin = 0.34x` |
| Window LAG OVER PARTITION p50 | `117.49 ms` | `30.45 ms` | **Postgres** | `pg / basin = 0.26x` |
| Recursive CTE Fibonacci(30) p50 | `9.82 ms` | `0.13 ms` | **Postgres** | `pg / basin = 0.01x` |
| Correlated subquery in SELECT p50 | `68.26 ms` | `312.73 ms` | **Basin** | `pg / basin = 4.58x` |
| EXISTS in WHERE p50 | `39.46 ms` | `17.21 ms` | **Postgres** | `pg / basin = 0.44x` |
| 3-table JOIN BETWEEN p50 | `119.92 ms` | `49.73 ms` | **Postgres** | `pg / basin = 0.41x` |
| UNION ALL two scans p50 | `80.12 ms` | `26.04 ms` | **Postgres** | `pg / basin = 0.33x` |
| ORDER BY NULLS LAST + LIMIT p50 | `13.25 ms` | `5.46 ms` | **Postgres** | `pg / basin = 0.41x` |
| Top-N per group (MAX) p50 | `142.49 ms` | `35.57 ms` | **Postgres** | `pg / basin = 0.25x` |
| Numeric range BETWEEN p50 | `22.09 ms` | `13.84 ms` | **Postgres** | `pg / basin = 0.63x` |

### Basin (Parquet) vs Postgres 18 (100k-row SaaS+OLAP workload, no index)

_Same 100k-row SaaS+OLAP workload as the Vortex card, but Basin is pinned to basin.file_format='parquet'. Shows how the legacy / Iceberg-read-compat Parquet path compares against both PG heap and the Vortex-default card._

| Metric | Basin | Postgres | Winner | Ratio |
|---|---|---|---|---|
| On-disk bytes (users + events) | `1.02 MiB` | `7.73 MiB` | **Basin** | `pg / basin = 7.57x` |
| Point query p50 | `0.95 ms` | `2.56 ms` | **Basin** | `pg / basin = 2.69x` |
| Point query p99 | `22.64 ms` | `2.81 ms` | **Postgres** | `pg / basin = 0.12x` |
| Range scan p50 (~1k rows) | `0.90 ms` | `5.33 ms` | **Basin** | `pg / basin = 5.91x` |
| Range scan p99 | `25.60 ms` | `5.38 ms` | **Postgres** | `pg / basin = 0.21x` |
| Aggregate GROUP BY user_id p50 | `48.57 ms` | `13.30 ms` | **Postgres** | `pg / basin = 0.27x` |
| 2-table JOIN GROUP BY p50 | `104.86 ms` | `44.90 ms` | **Postgres** | `pg / basin = 0.43x` |
| ILIKE '%@gmail.com' p50 | `11.24 ms` | `2.58 ms` | **Postgres** | `pg / basin = 0.23x` |
| Pagination ORDER BY LIMIT/OFFSET p50 | `123.25 ms` | `16.99 ms` | **Postgres** | `pg / basin = 0.14x` |
| Single-row UPDATE p50 | `60.59 ms` | `0.34 ms` | **Postgres** | `pg / basin = 0.01x` |
| Bulk UPDATE (~33333 rows) | `14270.71 ms` | `68.27 ms` | **Postgres** | `—` |
| DELETE WHERE id IN (10 rows) | `14214.01 ms` | `5.98 ms` | **Postgres** | `—` |
| Bulk INSERT 100000 rows | `9196.80 ms` | `401.90 ms` | **Postgres** | `—` |
| Cold-start first query | `4.33 ms` | `14.86 ms` | **Basin** | `pg / basin = 3.43x` |
| COUNT(*) full table p50 | `0.55 ms` | `4.95 ms` | **Basin** | `pg / basin = 8.98x` |
| DATE_TRUNC day + SUM GROUP BY p50 | `57.75 ms` | `18.22 ms` | **Postgres** | `pg / basin = 0.32x` |
| Analytics JOIN+WHERE (last 33333s window) | `60.91 ms` | `60.37 ms` | **Postgres** | `pg / basin = 0.99x` |
| COUNT(DISTINCT user_id) p50 | `35.85 ms` | `33.96 ms` | **Postgres** | `pg / basin = 0.95x` |
| LIKE 'pending%' prefix p50 | `42.37 ms` | `0.26 ms` | **Postgres** | `pg / basin = 0.01x` |
| Multi-col GROUP BY + HAVING p50 | `99.90 ms` | `44.19 ms` | **Postgres** | `pg / basin = 0.44x` |
| Window LAG OVER PARTITION p50 | `149.74 ms` | `30.75 ms` | **Postgres** | `pg / basin = 0.21x` |
| Recursive CTE Fibonacci(30) p50 | `10.88 ms` | `0.14 ms` | **Postgres** | `pg / basin = 0.01x` |
| Correlated subquery in SELECT p50 | `44.12 ms` | `308.35 ms` | **Basin** | `pg / basin = 6.99x` |
| EXISTS in WHERE p50 | `44.72 ms` | `14.47 ms` | **Postgres** | `pg / basin = 0.32x` |
| 3-table JOIN BETWEEN p50 | `126.62 ms` | `40.76 ms` | **Postgres** | `pg / basin = 0.32x` |
| UNION ALL two scans p50 | `78.14 ms` | `25.79 ms` | **Postgres** | `pg / basin = 0.33x` |
| ORDER BY NULLS LAST + LIMIT p50 | `14.67 ms` | `6.77 ms` | **Postgres** | `pg / basin = 0.46x` |
| Top-N per group (MAX) p50 | `149.24 ms` | `29.70 ms` | **Postgres** | `pg / basin = 0.20x` |
| Numeric range BETWEEN p50 | `20.73 ms` | `15.53 ms` | **Postgres** | `pg / basin = 0.75x` |

### Basin vs Postgres 18 (10k-row SaaS+OLAP workload, no index)

_At small-project scale (10k rows = 1k users + 10k events), the entire working set fits in PG's shared_buffers, so PG wins more of the OLTP latency metrics. Basin still leads on the OLAP shapes (COUNT(*), DATE_TRUNC rollup, analytics JOIN) where the win is algorithmic. Honest small-scale baseline for the 100k / 1M siblings._

| Metric | Basin | Postgres | Winner | Ratio |
|---|---|---|---|---|
| On-disk bytes (users + events) | `1.46 MiB` | `2.75 MiB` | **Basin** | `pg / basin = 1.88x` |
| Point query p50 | `1.25 ms` | `0.31 ms` | **Postgres** | `pg / basin = 0.25x` |
| Point query p99 | `4.84 ms` | `0.32 ms` | **Postgres** | `pg / basin = 0.07x` |
| Range scan p50 (~1k rows) | `1.09 ms` | `0.68 ms` | **Postgres** | `pg / basin = 0.62x` |
| Range scan p99 | `3.81 ms` | `0.82 ms` | **Postgres** | `pg / basin = 0.21x` |
| Aggregate GROUP BY user_id p50 | `10.93 ms` | `1.26 ms` | **Postgres** | `pg / basin = 0.11x` |
| 2-table JOIN GROUP BY p50 | `17.93 ms` | `3.16 ms` | **Postgres** | `pg / basin = 0.18x` |
| ILIKE '%@gmail.com' p50 | `4.67 ms` | `0.26 ms` | **Postgres** | `pg / basin = 0.06x` |
| Pagination ORDER BY LIMIT/OFFSET p50 | `14.56 ms` | `1.72 ms` | **Postgres** | `pg / basin = 0.12x` |
| Single-row UPDATE p50 | `71.75 ms` | `0.04 ms` | **Postgres** | `pg / basin = 0.00x` |
| Bulk UPDATE (~3333 rows) | `176.20 ms` | `6.51 ms` | **Postgres** | `—` |
| DELETE WHERE id IN (10 rows) | `176.24 ms` | `0.98 ms` | **Postgres** | `—` |
| Bulk INSERT 10000 rows | `2293.00 ms` | `94.65 ms` | **Postgres** | `—` |
| Cold-start first query | `15.97 ms` | `10.41 ms` | **Postgres** | `pg / basin = 0.65x` |
| COUNT(*) full table p50 | `0.23 ms` | `0.53 ms` | **Basin** | `pg / basin = 2.28x` |
| DATE_TRUNC day + SUM GROUP BY p50 | `8.21 ms` | `1.89 ms` | **Postgres** | `pg / basin = 0.23x` |
| Analytics JOIN+WHERE (last 3333s window) | `16.54 ms` | `1.78 ms` | **Postgres** | `pg / basin = 0.11x` |
| COUNT(DISTINCT user_id) p50 | `7.36 ms` | `3.35 ms` | **Postgres** | `pg / basin = 0.46x` |
| LIKE 'pending%' prefix p50 | `1.06 ms` | `0.10 ms` | **Postgres** | `pg / basin = 0.09x` |
| Multi-col GROUP BY + HAVING p50 | `16.53 ms` | `4.08 ms` | **Postgres** | `pg / basin = 0.25x` |
| Window LAG OVER PARTITION p50 | `25.32 ms` | `4.72 ms` | **Postgres** | `pg / basin = 0.19x` |
| Recursive CTE Fibonacci(30) p50 | `7.18 ms` | `0.04 ms` | **Postgres** | `pg / basin = 0.01x` |
| Correlated subquery in SELECT p50 | `15.15 ms` | `33.59 ms` | **Basin** | `pg / basin = 2.22x` |
| EXISTS in WHERE p50 | `9.22 ms` | `1.83 ms` | **Postgres** | `pg / basin = 0.20x` |
| 3-table JOIN BETWEEN p50 | `22.28 ms` | `9.73 ms` | **Postgres** | `pg / basin = 0.44x` |
| UNION ALL two scans p50 | `9.42 ms` | `1.21 ms` | **Postgres** | `pg / basin = 0.13x` |
| ORDER BY NULLS LAST + LIMIT p50 | `4.60 ms` | `0.29 ms` | **Postgres** | `pg / basin = 0.06x` |
| Top-N per group (MAX) p50 | `17.98 ms` | `3.30 ms` | **Postgres** | `pg / basin = 0.18x` |
| Numeric range BETWEEN p50 | `5.92 ms` | `1.02 ms` | **Postgres** | `pg / basin = 0.17x` |
| JSONB -> get key p50 | `7.09 ms` | `0.93 ms` | **Postgres** | `pg / basin = 0.13x` |
| JSONB ->> get text p50 | `6.38 ms` | `0.90 ms` | **Postgres** | `pg / basin = 0.14x` |
| JSONB -> deep path (device.version) p50 (basin gap) | `-1.00 ms` | `0.89 ms` | **Postgres** | `basin: unsupported` |
| JSONB @> contains p50 (no GIN index) | `84.71 ms` | `2.49 ms` | **Postgres** | `pg / basin = 0.03x` |
| JSONB @> contains p50 (with GIN index) | `83.47 ms` | `1.02 ms` | **Postgres** | `pg / basin = 0.01x` |
| GIN @> effectiveness (no-index ms / with-index ms) | `1.01 ratio` | `2.43 ratio` | **Basin** | `basin 1.01x vs pg 2.43x speedup (higher = better)` |
| JSONB ? key exists p50 | `74.78 ms` | `2.52 ms` | **Postgres** | `pg / basin = 0.03x` |
| JSONB #> path get p50 | `7.29 ms` | `2.14 ms` | **Postgres** | `pg / basin = 0.29x` |
| JSONB jsonb_array_length(tags) p50 | `6.79 ms` | `1.78 ms` | **Postgres** | `pg / basin = 0.26x` |
| JSONB jsonb_typeof(metadata) p50 | `6.89 ms` | `2.05 ms` | **Postgres** | `pg / basin = 0.30x` |
| JSONB filter+agg (GROUP BY ->>category) p50 (basin gap) | `-1.00 ms` | `7.05 ms` | **Postgres** | `basin: unsupported` |
| JSONB jsonb_set UPDATE (10 rows) (basin gap) | `-1.00 ms` | `0.97 ms` | **Postgres** | `basin: unsupported` |

### Basin (Parquet) vs Postgres 18 (10k-row SaaS+OLAP workload, no index)

_Same 10k-row small-project SaaS+OLAP workload as the Vortex card, but Basin is pinned to basin.file_format='parquet'. Honest small-scale baseline for the Parquet path against PG heap and the Vortex card._

| Metric | Basin | Postgres | Winner | Ratio |
|---|---|---|---|---|
| On-disk bytes (users + events) | `0.10 MiB` | `0.84 MiB` | **Basin** | `pg / basin = 8.77x` |
| Point query p50 | `0.68 ms` | `0.27 ms` | **Postgres** | `pg / basin = 0.40x` |
| Point query p99 | `5.04 ms` | `0.28 ms` | **Postgres** | `pg / basin = 0.05x` |
| Range scan p50 (~1k rows) | `0.68 ms` | `0.54 ms` | **Postgres** | `pg / basin = 0.80x` |
| Range scan p99 | `5.91 ms` | `0.60 ms` | **Postgres** | `pg / basin = 0.10x` |
| Aggregate GROUP BY user_id p50 | `10.31 ms` | `1.26 ms` | **Postgres** | `pg / basin = 0.12x` |
| 2-table JOIN GROUP BY p50 | `18.42 ms` | `3.13 ms` | **Postgres** | `pg / basin = 0.17x` |
| ILIKE '%@gmail.com' p50 | `3.16 ms` | `0.27 ms` | **Postgres** | `pg / basin = 0.08x` |
| Pagination ORDER BY LIMIT/OFFSET p50 | `15.27 ms` | `1.72 ms` | **Postgres** | `pg / basin = 0.11x` |
| Single-row UPDATE p50 | `8.31 ms` | `0.04 ms` | **Postgres** | `pg / basin = 0.01x` |
| Bulk UPDATE (~3333 rows) | `81.96 ms` | `3.20 ms` | **Postgres** | `—` |
| DELETE WHERE id IN (10 rows) | `70.66 ms` | `0.75 ms` | **Postgres** | `—` |
| Bulk INSERT 10000 rows | `874.17 ms` | `45.41 ms` | **Postgres** | `—` |
| Cold-start first query | `5.08 ms` | `9.19 ms` | **Basin** | `pg / basin = 1.81x` |
| COUNT(*) full table p50 | `0.23 ms` | `0.52 ms` | **Basin** | `pg / basin = 2.21x` |
| DATE_TRUNC day + SUM GROUP BY p50 | `8.84 ms` | `1.87 ms` | **Postgres** | `pg / basin = 0.21x` |
| Analytics JOIN+WHERE (last 3333s window) | `18.90 ms` | `1.78 ms` | **Postgres** | `pg / basin = 0.09x` |
| COUNT(DISTINCT user_id) p50 | `6.10 ms` | `4.33 ms` | **Postgres** | `pg / basin = 0.71x` |
| LIKE 'pending%' prefix p50 | `0.64 ms` | `0.14 ms` | **Postgres** | `pg / basin = 0.22x` |
| Multi-col GROUP BY + HAVING p50 | `15.82 ms` | `4.59 ms` | **Postgres** | `pg / basin = 0.29x` |
| Window LAG OVER PARTITION p50 | `28.79 ms` | `4.98 ms` | **Postgres** | `pg / basin = 0.17x` |
| Recursive CTE Fibonacci(30) p50 | `6.44 ms` | `0.09 ms` | **Postgres** | `pg / basin = 0.01x` |
| Correlated subquery in SELECT p50 | `10.98 ms` | `32.38 ms` | **Basin** | `pg / basin = 2.95x` |
| EXISTS in WHERE p50 | `9.13 ms` | `1.31 ms` | **Postgres** | `pg / basin = 0.14x` |
| 3-table JOIN BETWEEN p50 | `21.50 ms` | `7.59 ms` | **Postgres** | `pg / basin = 0.35x` |
| UNION ALL two scans p50 | `13.04 ms` | `0.93 ms` | **Postgres** | `pg / basin = 0.07x` |
| ORDER BY NULLS LAST + LIMIT p50 | `4.21 ms` | `0.76 ms` | **Postgres** | `pg / basin = 0.18x` |
| Top-N per group (MAX) p50 | `20.27 ms` | `3.88 ms` | **Postgres** | `pg / basin = 0.19x` |
| Numeric range BETWEEN p50 | `7.92 ms` | `1.92 ms` | **Postgres** | `pg / basin = 0.24x` |

### Basin (Parquet) vs Postgres 18 (1M-row SaaS+OLAP workload, no index)

_Same 1M-row SaaS+OLAP workload as the Vortex card, but Basin is pinned to basin.file_format='parquet'. Shows how the legacy / Iceberg-read-compat Parquet path compares against both PG heap and the Vortex-default card._

| Metric | Basin | Postgres | Winner | Ratio |
|---|---|---|---|---|
| On-disk bytes (users + events) | `13.48 MiB` | `76.73 MiB` | **Basin** | `pg / basin = 5.69x` |
| Point query p50 | `0.84 ms` | `12.33 ms` | **Basin** | `pg / basin = 14.64x` |
| Point query p99 | `23.96 ms` | `12.41 ms` | **Postgres** | `pg / basin = 0.52x` |
| Range scan p50 (~1k rows) | `0.80 ms` | `22.00 ms` | **Basin** | `pg / basin = 27.40x` |
| Range scan p99 | `26.43 ms` | `22.09 ms` | **Postgres** | `pg / basin = 0.84x` |
| Aggregate GROUP BY user_id p50 | `514.11 ms` | `131.13 ms` | **Postgres** | `pg / basin = 0.26x` |
| 2-table JOIN GROUP BY p50 | `1057.29 ms` | `919.69 ms` | **Postgres** | `pg / basin = 0.87x` |
| ILIKE '%@gmail.com' p50 | `65.31 ms` | `24.43 ms` | **Postgres** | `pg / basin = 0.37x` |
| Pagination ORDER BY LIMIT/OFFSET p50 | `1220.20 ms` | `62.43 ms` | **Postgres** | `pg / basin = 0.05x` |
| Single-row UPDATE p50 | `3984.35 ms` | `2.33 ms` | **Postgres** | `pg / basin = 0.00x` |
| Bulk UPDATE (~333333 rows) | `86014.87 ms` | `1306.28 ms` | **Postgres** | `—` |
| DELETE WHERE id IN (10 rows) | `106491.83 ms` | `52.09 ms` | **Postgres** | `—` |
| Bulk INSERT 1000000 rows | `210621.61 ms` | `4420.81 ms` | **Postgres** | `—` |
| Cold-start first query | `4.34 ms` | `21.24 ms` | **Basin** | `pg / basin = 4.89x` |
| COUNT(*) full table p50 | `0.25 ms` | `20.58 ms` | **Basin** | `pg / basin = 82.87x` |
| DATE_TRUNC day + SUM GROUP BY p50 | `488.40 ms` | `67.90 ms` | **Postgres** | `pg / basin = 0.14x` |
| Analytics JOIN+WHERE (last 333333s window) | `589.03 ms` | `156.03 ms` | **Postgres** | `pg / basin = 0.26x` |
| COUNT(DISTINCT user_id) p50 | `346.79 ms` | `194.93 ms` | **Postgres** | `pg / basin = 0.56x` |
| LIKE 'pending%' prefix p50 | `0.95 ms` | `0.17 ms` | **Postgres** | `pg / basin = 0.18x` |
| Multi-col GROUP BY + HAVING p50 | `955.74 ms` | `221.34 ms` | **Postgres** | `pg / basin = 0.23x` |
| Window LAG OVER PARTITION p50 | `830.25 ms` | `74.51 ms` | **Postgres** | `pg / basin = 0.09x` |
| Recursive CTE Fibonacci(30) p50 | `6.55 ms` | `0.10 ms` | **Postgres** | `pg / basin = 0.01x` |
| Correlated subquery in SELECT p50 | `375.17 ms` | `2728.23 ms` | **Basin** | `pg / basin = 7.27x` |
| EXISTS in WHERE p50 | `402.05 ms` | `139.03 ms` | **Postgres** | `pg / basin = 0.35x` |
| 3-table JOIN BETWEEN p50 | `1303.87 ms` | `257.57 ms` | **Postgres** | `pg / basin = 0.20x` |
| UNION ALL two scans p50 | `542.44 ms` | `65.16 ms` | **Postgres** | `pg / basin = 0.12x` |
| ORDER BY NULLS LAST + LIMIT p50 | `87.34 ms` | `18.08 ms` | **Postgres** | `pg / basin = 0.21x` |
| Top-N per group (MAX) p50 | `893.12 ms` | `133.41 ms` | **Postgres** | `pg / basin = 0.15x` |
| Numeric range BETWEEN p50 | `18.02 ms` | `19.09 ms` | **Basin** | `pg / basin = 1.06x` |

### Server lifecycle: accept, conn-scaling, RSS/conn

_Basin's tokio-task model handles dramatically more connections at lower memory than PG's process-per-connection._

| Metric | Basin | Postgres | Winner | Ratio |
|---|---|---|---|---|
| Connection accept latency p50 | `0.72 ms` | `1.23 ms` | **Basin** | `pg / basin = 1.71x` |
| Connections held under 1000-conn flood | `1000.00 conns` | `99.00 conns` | **Basin** | `basin / pg = 10.10x` |
| Refused conns under 1000-conn flood | `0.00 conns` | `901.00 conns` | **Basin** | `—` |
| RSS per held-open connection | `309.12 KiB` | `7658.08 KiB` | **Basin** | `pg / basin = 24.77x` |

### Parquet (ZSTD) vs Vortex — audit-log 1M rows

_Vortex achieves competitive compression and fast full-scan vs ZSTD Parquet on audit-log data._

| Metric | Basin | Postgres | Winner | Ratio |
|---|---|---|---|---|
| On-disk size | `3.03 MiB` | `1.55 MiB` | **Postgres** | `parquet / vortex = 1.95x` |
| Full-scan wall time | `61.75 ms` | `3.25 ms` | **Postgres** | `parquet / vortex = 18.99x` |

> JSON key 'basin'=Parquet, 'postgres'=Vortex (schema reuse from Postgres compare card). Standalone binary: cargo run --manifest-path benchmark/vortex_compare/Cargo.toml --release

## What to read next

- [`README.md`](../README.md) — what Basin is, who it's for, how to try it.
- [`WEDGE.md`](../WEDGE.md) — six-month wedge-deepening roadmap.
- [`CAPABILITIES.md`](../CAPABILITIES.md) — public capability matrix.
- [`docs/architecture.md`](../docs/architecture.md) — the four-layer stack.
- [`docs/decisions/`](../docs/decisions/) — every "no" we've recorded, with the trigger that would change our mind.
