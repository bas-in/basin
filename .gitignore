/target
.claude/
**/*.rs.bk
Cargo.lock.bak
.DS_Store
.env
.envrc
.idea/
.vscode/
*.profraw
/tmp/

# Per-developer test config — contains real cloud credentials.
# NEVER commit this file. The example template lives at .basin-test.example.toml.
.basin-test.toml
basin-test.toml

# 3-way cloud benchmark config — contains real Neon/Supabase/Basin Cloud DSNs.
# NEVER commit. The example template lives at .basin-three-way.example.toml.
.basin-three-way.toml

.basin-local-s3-data
.basin-local-s3.pid
.basin-local-s3.log
**/target/
.basin-seaweedfs-data/
.basin-seaweedfs-*
