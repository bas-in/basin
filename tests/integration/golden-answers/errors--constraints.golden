# Basin golden answers — recorded from the INCUMBENT engine (BASIN_OWNED_ENGINE unset).
# suite: errors   area: Constraints
# Do not hand-edit. Regenerate: BASIN_GOLDEN_RECORD=1 cargo test -p basin-integration-tests \
#   --test golden_answers -- --ignored --nocapture record_golden_answers
# Rows are sorted unless ORDER is `as-returned`. `\N` is NULL. Cells are tab-separated.

QUERY 0020
AREA Constraints
SQL INSERT INTO p VALUES (1,1,'dup')
ORDER sorted
STATUS error
ERROR duplicate key value violates unique constraint "p_pkey": Key (id)=(1) already exists.
END

QUERY 0021
AREA Constraints
SQL INSERT INTO p (id, val) VALUES (9, NULL)
ORDER sorted
STATUS error
ERROR NULL inserted into NOT NULL column val
END

QUERY 0022
AREA Constraints
SQL INSERT INTO t VALUES (1)
ORDER sorted
STATUS error
ERROR invalid schema: row 0 has 1 values, expected 3
END

QUERY 0023
AREA Constraints
SQL CREATE TABLE t (x INTEGER)
ORDER sorted
STATUS error
ERROR catalog error: table <PROJECT>/public.t already exists
END

