# Basin golden answers — recorded from the INCUMBENT engine (BASIN_OWNED_ENGINE unset).
# suite: errors   area: Fixture
# Do not hand-edit. Regenerate: BASIN_GOLDEN_RECORD=1 cargo test -p basin-integration-tests \
#   --test golden_answers -- --ignored --nocapture record_golden_answers
# Rows are sorted unless ORDER is `as-returned`. `\N` is NULL. Cells are tab-separated.

QUERY 0001
AREA Fixture
SQL CREATE TABLE t (id BIGINT NOT NULL, name TEXT, amt DOUBLE PRECISION)
ORDER sorted
STATUS tag
TAG CREATE TABLE
END

QUERY 0002
AREA Fixture
SQL INSERT INTO t VALUES (1,'a',1.5),(2,'b',2.5),(3,'c',3.5)
ORDER sorted
STATUS tag
TAG INSERT 0 3
END

QUERY 0003
AREA Fixture
SQL CREATE TABLE d (id BIGINT NOT NULL, day DATE, ts TIMESTAMP, flag BOOLEAN)
ORDER sorted
STATUS tag
TAG CREATE TABLE
END

QUERY 0004
AREA Fixture
SQL CREATE TABLE p (id BIGINT PRIMARY KEY, val INTEGER NOT NULL DEFAULT 0, tag TEXT)
ORDER sorted
STATUS tag
TAG CREATE TABLE
END

QUERY 0005
AREA Fixture
SQL INSERT INTO p VALUES (1,10,'a'),(2,20,'b')
ORDER sorted
STATUS tag
TAG INSERT 0 2
END

